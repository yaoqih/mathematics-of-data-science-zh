# 协作指南

## 提交范围

每次提交以一个自然小节为宜，同时更新章节状态。不要在翻译提交中混入主题重构、图片批处理或上游原稿修改。

## 检查清单

提交前确认：

- `make check` 通过；
- 公式可以由 MathJax 正确渲染；
- 内部链接、图片和引用可访问；
- 术语符合 `glossary/terms.yml`；
- 没有改动 `upstream/en-latex/` 中的原稿；
- 移动端没有横向溢出，宽公式允许局部滚动。

英文原稿更新应作为独立提交进行，并在 `upstream/en-latex/VERSION.md` 记录来源与差异。

